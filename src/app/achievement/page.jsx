import React from 'react'
import AchievementWraper from '../../components/achievements/AchievementWraper'
export default function page() { // Defining the main functional component named 'Footer'.

    return (
        <AchievementWraper />
    )
}
